# 多语言 (i18n) 设置指南

## 📦 安装依赖

首先需要安装以下 npm 包：

```bash
npm install i18next react-i18next i18next-browser-languagedetector --save
```

## ✅ 已完成的设置

### 1. i18n 配置
- ✅ 创建了 `src/lib/i18n/config.ts` - i18n 配置文件
- ✅ 配置了语言检测（localStorage、浏览器、HTML 标签）
- ✅ 设置了默认语言为中文（zh-CN）

### 2. 翻译文件
- ✅ 创建了 `src/lib/i18n/locales/zh-CN.json` - 中文翻译
- ✅ 创建了 `src/lib/i18n/locales/en-US.json` - 英文翻译
- ✅ 包含了所有主要页面的翻译键

### 3. 组件
- ✅ 创建了 `src/components/LanguageSwitcher.tsx` - 语言切换组件
- ✅ 更新了 `src/components/Navbar.tsx` - 集成语言切换和翻译
- ✅ 更新了 `src/App.tsx` - 初始化 i18n

## 📋 翻译键结构

翻译文件按照页面和功能组织：

```json
{
  "common": {},      // 通用文本
  "navbar": {},      // 导航栏
  "home": {},        // 首页
  "gameLab": {},     // 游戏实验室
  "auth": {},        // 认证
  "creations": {},   // 创作
  "community": {},   // 社区
  "creation": {},    // 创作详情
  "studio": {},      // 工作室
  "errors": {}       // 错误消息
}
```

## 🔧 使用方法

### 在组件中使用翻译

```tsx
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('home.title')}</h1>
      <p>{t('home.subtitle')}</p>
    </div>
  );
}
```

### 切换语言

语言切换组件已经集成到导航栏，用户可以通过点击语言按钮切换语言。

## 🚀 下一步：更新所有组件

需要将所有组件中的硬编码中文文本替换为翻译函数：

### 需要更新的组件

1. **页面组件**：
   - [ ] `src/pages/Index.tsx`
   - [ ] `src/pages/GameLab.tsx`
   - [ ] `src/pages/MyCreations.tsx`
   - [ ] `src/pages/CreationPage.tsx`
   - [ ] `src/pages/Community.tsx`
   - [ ] `src/pages/StudioPage.tsx`

2. **组件**：
   - [ ] `src/components/AICreator.tsx`
   - [ ] `src/components/WorkGallery.tsx`
   - [ ] `src/components/CommentsSection.tsx`
   - [ ] `src/components/AuthModal.tsx`
   - [ ] 其他组件...

### 更新步骤

1. 在组件顶部添加：
   ```tsx
   import { useTranslation } from 'react-i18next';
   const { t } = useTranslation();
   ```

2. 替换硬编码文本：
   ```tsx
   // 之前
   <h1>随心创造,分享快乐</h1>
   
   // 之后
   <h1>{t('home.title')}</h1>
   ```

3. 如果翻译键不存在，先在翻译文件中添加，然后使用。

## 📝 添加新翻译

如果需要添加新的翻译：

1. 在 `zh-CN.json` 中添加中文翻译
2. 在 `en-US.json` 中添加对应的英文翻译
3. 在组件中使用 `t('your.new.key')`

## 🔍 检查缺失的翻译

运行开发服务器后，如果看到翻译键本身（如 `home.title`）而不是翻译文本，说明该翻译键不存在，需要添加到翻译文件中。

## 💡 提示

- 翻译键使用点号分隔的层级结构，便于组织
- 保持翻译文件的结构一致（中英文）
- 使用有意义的键名，便于维护
- 可以为相同的文本使用相同的翻译键，避免重复

## 🎯 完成度

- ✅ i18n 基础配置：100%
- ✅ 导航栏翻译：100%
- ⚠️ 其他组件翻译：0%（待完成）
