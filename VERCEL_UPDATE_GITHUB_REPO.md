# 在 Vercel 中更改连接的 GitHub 项目

## 方法一：在项目设置中更改（推荐）

### 步骤：

1. **登录 Vercel Dashboard**
   - 访问 https://vercel.com/dashboard
   - 登录你的账户

2. **进入项目设置**
   - 找到并点击你的项目（选错仓库的那个项目）
   - 进入项目详情页
   - 点击顶部或侧边栏的 **"Settings"**（设置）

3. **修改 Git Repository**
   - 在 Settings 页面中，找到 **"Git"** 或 **"General"** 部分
   - 找到 **"Git Repository"** 设置项
   - 点击 **"Disconnect"**（断开连接）按钮
   - 确认断开连接

4. **连接正确的仓库**
   - 点击 **"Connect Git Repository"** 或 **"Connect Repository"**
   - 选择 GitHub（如果还没授权，需要先授权）
   - 搜索并选择正确的仓库：`creatbyvibe/enjoy-byvibe`
   - 点击 **"Import"** 或 **"Connect"**

5. **确认设置**
   - 检查项目配置（Root Directory、Build Command、Output Directory 等）
   - 确保 Build Command: `npm run build`
   - 确保 Output Directory: `dist`（Vite 项目的默认输出）
   - 点击 **"Deploy"** 开始部署

## 方法二：删除并重新创建项目

如果方法一不行，可以删除旧项目并重新创建：

### 步骤：

1. **删除旧项目**
   - 在 Vercel Dashboard 中找到选错仓库的项目
   - 进入项目 Settings
   - 滚动到底部，找到 **"Danger Zone"**（危险区域）
   - 点击 **"Delete Project"**
   - 输入项目名称确认删除

2. **创建新项目**
   - 点击 Dashboard 的 **"Add New..."** → **"Project"**
   - 在 Import Git Repository 中搜索 `creatbyvibe/enjoy-byvibe`
   - 选择正确的仓库
   - 配置项目：
     - Framework Preset: **Vite**
     - Root Directory: `./`（默认）
     - Build Command: `npm run build`
     - Output Directory: `dist`
     - Install Command: `npm install`
   - 点击 **"Deploy"**

## 方法三：使用 Vercel CLI

如果你想用命令行操作：

```bash
# 安装 Vercel CLI（如果还没安装）
npm i -g vercel

# 登录 Vercel
vercel login

# 链接到现有项目（需要项目名称或 ID）
vercel link

# 或者在项目目录中直接部署，会自动提示连接仓库
cd /Users/wubinyuan/enjoy-byvibe
vercel
```

## 📋 检查清单

更改后请确认：

- [ ] ✅ Git Repository 显示正确的仓库：`creatbyvibe/enjoy-byvibe`
- [ ] ✅ Build Command: `npm run build`
- [ ] ✅ Output Directory: `dist`
- [ ] ✅ Framework Preset: `Vite`
- [ ] ✅ 环境变量已正确配置（如果有）
- [ ] ✅ 部署成功，网站可以正常访问

## 🔧 正确的 Vite 项目配置

确保 Vercel 项目设置如下：

| 设置项 | 值 |
|--------|-----|
| Framework Preset | Vite |
| Root Directory | `./` |
| Build Command | `npm run build` |
| Output Directory | `dist` |
| Install Command | `npm install` |
| Development Command | `npm run dev` |

## ⚠️ 注意事项

1. **环境变量**：如果旧项目有环境变量，记得在新项目中重新配置
2. **域名设置**：如果配置了自定义域名，需要在 Settings → Domains 中重新添加
3. **自动部署**：连接正确的仓库后，每次推送到 `main` 分支都会自动部署

## 💡 推荐操作流程

1. 先尝试**方法一**（在设置中修改）- 最简单
2. 如果不行，使用**方法二**（删除重建）- 最彻底
3. 确认连接正确后，推送到 GitHub 触发自动部署

## 🆘 如果遇到问题

- **找不到设置选项**：确保你是项目的 Owner 或 Admin
- **无法断开连接**：可能需要先删除现有的部署
- **部署失败**：检查 Build Command 和 Output Directory 是否正确
